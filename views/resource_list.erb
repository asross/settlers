<%
  card_data = []
  Player::RESOURCE_CARDS.each do |resource|
    n = resources.count{|r| r == resource }
    if n > 0
      card_data << [resource, n]
    end
  end
%>
<%= erb :card_list, locals: { card_data: card_data } %>
